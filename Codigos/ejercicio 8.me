#include <stdio.h>
#include "ejercicios.h"

void ejercicio8() {
    int N, R = 0;

    printf("Ingrese un numero positivo: ");
    scanf("%d", &N);

    int i = 1;
    while (i * i <= N) {
        R = i;
        i++;
    }

    printf("La raiz cuadrada entera por defecto de %d es: %d\n", N, R);
}
